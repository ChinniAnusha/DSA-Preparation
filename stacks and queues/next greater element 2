class Solution:
    def nextGreaterElements(self, nums: List[int]) -> List[int]:
        n = len(nums)
        result = []
        for i in range(n):
             x = nums[i]
             found = False
             for j in range(i+1,i+n):
                  if nums[j%n] > x:
                      result.append(nums[j%n])
                      found = True
                      break
             if not found:
                result.append(-1)
             
        return result

#TC:O(n^2)
#SC:O(n)
