class Solution:
    def singleNumber(self, arr: List[int]) -> int:
         n = len(arr)    #TC:O(n) and SC:O(1)
         xor = 0
         for i in range(n):
             xor = xor^arr[i]
         return xor
#TC:O(n)
#SC:O(1)
