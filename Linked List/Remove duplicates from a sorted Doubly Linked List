class Node:
    def __init__(self, data=0, next=None, prev=None):
        self.data = data
        self.next = next
        self.prev = prev


# Don't change the code above.


def removeDuplicates(head: Node) -> Node:
    # Write your code here
    pass
    temp = head
    while temp != None and temp.next is not None:
        nextnode = temp.next
        while nextnode != None and temp.data == nextnode.data:
            nextnode = nextnode.next
        temp.next = nextnode
        if nextnode:
          nextnode.prev = temp
        temp = temp.next
    return head
            
