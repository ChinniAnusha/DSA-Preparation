#Approach:solved using upper bound
#floor and ceil of x in a sorted array:floor is largest number in the array <= x and ceil smallest number in array >= x
def floor(arr,n,x):
    answer = -1
    low = 0
    high = n-1
    while low <= high:
        mid = (low+high)//2
        if arr[mid] <= x:
            answer = arr[mid]
            low = mid + 1
        else:
            high = mid -1
    return answer
    
    
def ceil(arr,n,x):
    answer = arr[n-1]
    low = 0
    high = n - 1
    while low <= high:
        mid = (low+high)//2
        if x == arr[mid]:
            return arr[mid]
        elif arr[mid] > x:
            answer = arr[mid]
            high = mid - 1
        else:
            low = mid + 1
    return answer
    
    
arr = list(map(int,input().split()))
n = len(arr)   
x = int(input())
f = floor(arr,n,x)
c = ceil(arr,n,x)
print(f,c)

#TC:O(logN)
#SC:O(1)







